- name: Import cleanup playbook
  import_playbook: cleanup.yml

- name: Get commited configuration of devices
  hosts: all
  roles:
    - Juniper.junos
  gather_facts: no

  tasks:

    - name: Retrieve config + save at ~/ansible_tmp/{hostname}/{hostname}.config
      juniper_junos_config:
        retrieve: "committed"
        dest_dir: "{{ build_dir }}"
      register: response
#    - name: Print result
#      debug:
#        var: response.config_lines
