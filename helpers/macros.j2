{# makro zum auslesen und mergen der vlan-bereiche in ein einzelnes dict #}
{% macro merge_vlans(areas, merged) %}
{% for area in areas %}
    {% for vln in vlan_areas[area] %}
        {% set _dummy = merged.update({vln:vlan_areas[area][vln]}) %}
    {% endfor %}
{% endfor %}
{% endmacro %}